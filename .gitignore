__pycache__/
*.pyc
.venv/
.env
.DS_Store
*.nc
*.h5
*.tif
*.csv
*.zip
data/
.claude/
test_*.py
# Whitelisted: not a throwaway probe. It is the only coverage for the m=None print
# branch and the DDP sum-reduction, neither of which a smoke run reliably exercises.
!test_per_depth_loss.py
old_*.py
plots/
latex/
*.md
!text/architecture.md
wandb/

# Derived analysis output — regenerable from the scripts that write them
capacity_output/
embed_viz_output/
tier1_output/
graphify-out/
# Stray editor/atomic-write temporaries
*.tmp.*

# SLURM job output — some are 16 MB; the runbook records what mattered
logs/
# Prediction dumps and figures — regenerable from eval_predict.py / plot_eval_*.py.
# manifest.json is kept: it is the index of which artefacts exist.
eval_output/*.parquet
eval_output/timeseries/
figures/
demo_output/
meeting_output/
.ipynb_checkpoints/
